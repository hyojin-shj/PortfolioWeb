/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  // Optional: add any other existing config you may have
};

module.exports = nextConfig;
